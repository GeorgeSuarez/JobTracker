# JobTracker

Simple web app to track the jobs I have applied to.

If you want to run this web app on your local machine then clone this repo and cd into the project
and run the following command:

```bash
npm run dev
```
